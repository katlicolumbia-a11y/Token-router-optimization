[project]
name = "token-router-optimization"
version = "0.1.0"
description = "Difficulty-aware model router that optimizes model choice for task stages."
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
token-router = "token_router.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
